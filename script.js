// This event fires as soon as the HTML is fully parsed
document.addEventListener("DOMContentLoaded", () => {
    // Create a text node or set body text
    document.body.textContent = "DOM load success";
});
